<!--User page-->

<!--ユーザのプロフィールを、ページ上部に記述-->
<%= render 'users/user_info', users: @user %>
<%= render ' books/post_book',books: @new_book %>

<% @books.each do |book| %><!--コントローラから渡された@books変数内から1つずつ取り出して、post_image変数へ格納-->
<div>
 
   <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %> <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
  <p><%= book.title %></p><!--book変数のtitleカラム(bookモデルに定義)-->
  <p><%= book.body %></p>
  <p><%= book.user.name %></p>
</div>
<% end %>