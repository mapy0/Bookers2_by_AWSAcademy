<div>
  <p><%= @book.title %></p>
  <p><%= @book.body %></p>
  <p><%= @book.user.name %></p>
  <% if @book.user == current_user %>
  <%= link_to "Delete", book_path(@book), method: :delete %>
  <% end %>
</div>